{{persona}}

---

## 작업 지시

아래 경로의 skill.md를 분석하여 multi-agent 구조를 파악하세요.

### 분석 대상
```
{{user.skill_path}}
```

파일을 읽고 아래 분석을 수행하세요.

---

## 분석 항목
1. 이 스킬의 최종 목적
2. 등장하는 agent 수와 각 agent의 역할
3. agent 간 데이터/메시지 흐름
4. 병렬로 실행되는 구간 식별
5. sub-agent 위임 패턴
6. 조건부 분기 로직
7. single-agent 변환 시 복잡도가 높은 구간

## 출력

다음 파일을 `output/01_parse/` 폴더에 저장하세요.

**skill_analysis.md** — 분석 문서
- 스킬 목적 및 최종 결과물
- Agent 목록 (이름, 역할, 사용 도구)
- 의존성 다이어그램 (텍스트)
- 병렬 실행 구간 목록
- 변환 시 주의할 복잡도 포인트

**DONE.md** — 작업 요약
- Agent 수
- 병렬 구간 수
- 가장 복잡한 변환 포인트
